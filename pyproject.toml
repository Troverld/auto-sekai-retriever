[project]
name = "auto-sekai-retriever"
version = "0.1.0"
description = "Offline pipeline for auto-sekai-retriever"
requires-python = ">=3.11"
dependencies = ["pytest>=8,<9"]

[tool.pytest.ini_options]
testpaths = ["tests"]
